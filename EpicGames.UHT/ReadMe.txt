This code contains the code to generate the GameFramework Library
that represents the UE5 API.

To use it, clone the current Unreal Engine Source from GitHub,
then install these files in Engine/Source/Programs/Shared/EpicGames.UHT.